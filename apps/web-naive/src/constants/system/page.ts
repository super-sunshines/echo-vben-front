import { ref } from 'vue';

export const ColPageProps = ref({
  leftCollapsedWidth: 4,
  leftCollapsible: false,
  leftMaxWidth: 25,
  leftMinWidth: 15,
  leftWidth: 12,
  rightWidth: 88,
  splitHandle: true,
  splitLine: true,
});
